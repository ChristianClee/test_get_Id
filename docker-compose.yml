version: '3'

services: 
  back:
    build: ./api
    container_name: back-end
    restart: always
    ports: 
      - "5000:5000"
    depends_on:
      - mongo




  mongo:
    image: mongo
    restart: always
    container_name: my-mongo
    ports:
      - 27017:27017



